# Frontend Environment Variables (template)
# Copy to .env and use .env as the single source of truth.

# API Configuration
VITE_API_URL=http://localhost:8000

# App Configuration
VITE_APP_NAME=InfoLens AI
VITE_APP_VERSION=1.0.0
VITE_DEBUG=false
